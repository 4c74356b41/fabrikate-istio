name: istio
generator: helm
path: "./tmp/istio-1.1.1/install/kubernetes/helm/istio"
hooks:
  before-install:
    - curl -OL https://github.com/istio/istio/releases/download/1.1.1/istio-1.1.1-linux.tar.gz -o istio.tar.gz
    - mkdir tmp
    - tar xvf istio.tar.gz -C tmp
  after-install:
    - rm istio.tar.gz
subcomponents:
  - name: istio-namespace
    generator: static
    path: ./manifests
  - name: istio-init
    generator: helm
    path: "./tmp/istio-1.1.1/install/kubernetes/helm/istio-init"
